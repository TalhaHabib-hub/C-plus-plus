/*	🌿 𝔸𝕝𝕝𝕒𝕙 🌿	*/
#include<iostream>
using namespace std;

int main(){
    // Polymorphism -> many/multiple form -or- one name and multiple forms
    //eg. function overloading
    // type1 compile time polymorphism :-> we achive it by two ways it means the object will be bound with the function call at the compile time , this decision is taken during the compile time that which function is going to run and this is also called early binding and and static binding,
    // the compile time polymorphism in C++ is achieved in two ways, first through function overloading and other through operator overloading
    // type2 Runtime polymoriphism :->after compilation when the code is running at that time we will understand which function is going to be run according to the type of your pointer. linking of function call is differ by it  this buddy is achieved through virtual functions
/*
    Polymorphism in C++ can be of two types:
    1. Compile time Polymorphism 
       Compile time Polymorphism in C++ is acheived using:
        1.1 - Function overloading
        1.2 - Operator overloading
    2. Run time polymorphism
       Run time polymorphism in C++ is acheived using:
        2.1 - Virtual functions

*/
    return 0;
}